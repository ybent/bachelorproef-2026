# HOGENT Thesis Workspace Rules
- `make_thesis.bat` uses Docker to build the main thesis (which imports `voorstel/`).
- `make_voorstel.bat` builds only the proposal.
- LaTeX source files are split between the `bachproef/` and `voorstel/` subdirectories.
- PDF and log outputs are generated inside the `output/` directory.